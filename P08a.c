#include <stdio.h>
#include <string.h>
#include<stdlib.h>
int main()
{
char arr[10],i,count;
system("clear");
printf("Enter a string to reverse\n");
scanf("%s",arr[i]);
for(i=0;arr[i]!='\0';i++) 
count++;
for(i=count-1;i>=0;i--)
printf("%c",arr[i]);        
}

/* OUTPUT
Enter a string to reverse
welcome
emoclew */